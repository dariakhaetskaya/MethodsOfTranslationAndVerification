// Написать фукнцию, преобразующую массив целых чисел в массив строк, например: [1, 2, 3] в ["1", "2", "3"], 
// распечатать этот массив. Использовать функции: map, i2s, strGlue, println.

import runtime;
import ds/array;
import lingo/pegcode/driver;

main(){
    numberArray : [int] = [1, 2, 3, 4, 5, 6];
    println(strGlue(map(numberArray, \i -> i2s(i)) , ","));
}
